name: kiroshi_mobile
description: "KIROSHI Tourist Safety Client — Keypoint Intelligence for Real-time Observation, Safety & Human Interaction"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_secure_storage: ^9.0.0
  intl: ^0.19.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  location: ^6.0.2
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

